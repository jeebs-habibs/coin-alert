name: Send notis Every 5 Minutes
on:
  schedule:
    - cron: '*/5 * * * *'  # Runs every 5 minutes
  workflow_dispatch:
jobs:
  fetch-prices:
    runs-on: ubuntu-latest
    steps:
      - name: Send Request to notification API
        env:
          API_KEY: ${{ secrets.API_KEY }}
        run: |
          curl -X GET https://www.sirennotify.com/api/checkPriceAlerts -H "Authorization: mQPnHxpyVI8AZMjpKPFbkWufvIegASh7BGFDCVBNJUYTG34565" -H "Content-Type: application/json"
